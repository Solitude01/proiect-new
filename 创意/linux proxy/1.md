✅ 开代理（你现在已经在用的方式，完全正确）
/usr/local/bin/mihomo -f /etc/mihomo/config.yaml


前台运行

日志可见

Ctrl + C = 立刻关闭

这是最安全、最可控的方式

✅ 后台开（如果你想释放终端）
nohup /usr/local/bin/mihomo -f /etc/mihomo/config.yaml > /tmp/mihomo.log 2>&1 &


关：

pkill mihomo


conda activate paddle_vl
conda deactivate

任务,命令
查看所有环境          conda env list 或 conda info --envs
查看当前环境安装的包   conda list
删除一个环境           conda remove -n 环境名 --all

(base) root@scc111:~# pkill mihomo
(base) root@scc111:~# lsof -i:7890
